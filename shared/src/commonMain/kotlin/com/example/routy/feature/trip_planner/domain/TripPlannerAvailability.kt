package com.example.routy.feature.trip_planner.domain

/** No planner is advertised until its request/response contract is captured. */
enum class TripPlannerAvailability {
    AwaitingVerifiedApi,
}

// TODO: Capture and document getPointsBetweenStations request/response before adding a planner repository.
